# Database Directory

This directory contains database migration documentation, initial schema scripts, and seeds for future phases.

- Migrations are managed via **Alembic** in `backend/alembic/`.
- PostgreSQL is the primary application database.
